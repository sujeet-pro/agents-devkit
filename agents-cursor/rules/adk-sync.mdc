---
description: Bidirectional 3P bridge for markdown — read remote as markdown OR push markdown to Confluence / Jira / GDoc / GH / Slack.
globs:
alwaysApply: false
---

# /adk-sync (Cursor rule)

When the user invokes `/adk-sync` or asks to publish / push to / fetch / sync, load the skill spec.

@{{ADK_REPO}}/AGENTS.md
@{{ADK_REPO}}/skills/adk-sync/SKILL.md
@{{ADK_REPO}}/shared/constitution.md
@{{ADK_REPO}}/shared/question-first.md
