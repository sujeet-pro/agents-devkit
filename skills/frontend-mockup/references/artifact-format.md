# `frontend-mockup` — artifact format

The deliverable lives at `.temp/task-<slug>/<artifact>.md` per the canonical layout in `@adk:temp-folder` (a.k.a. `adk-temp-folder`).

Specific artifact path for this skill: `.temp/task-<slug>/frontend-mockup.md` (or the closest canonical slot — see SKILL.md).
