# `mode-contract` — modes

This skill IS the mode contract. It is reference-only (`disable-model-invocation: true`).

If you are an agent looking to understand a mode, read the SKILL.md.
If you are a skill author defining new modes for your skill, you must STILL only declare values from the universal set: `[auto, review, fix]`.
