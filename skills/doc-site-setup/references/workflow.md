# `doc-site-setup` — workflow

(Detailed step-by-step expanded from the SKILL.md "Workflow" section. Add concrete commands, MCP calls, and validator gate references at each step.)

1. Phase 1 validator (pre-execution).
2. Confirm intent (or skip on `--auto`).
3. Run main workflow (per SKILL.md).
4. Phase 2 validator gates between sub-steps.
5. Produce artifact (per `references/artifact-format.md`).
6. Phase 3 validator (pre-handoff).
7. Final report (per `references/output-format.md`).
8. Phase 4 validator (post-execution).
