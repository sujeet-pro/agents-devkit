# `setup` — artifact format

The report is written to `.temp/reports/setup-<YYYY-MM-DD-HHMM>.md` and printed to stdout.

(Setup is not task-scoped; it lives in `.temp/reports/`, not under `.temp/task-<slug>/`.)
