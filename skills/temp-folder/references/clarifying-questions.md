# `temp-folder` — clarifying questions

Only one ever:

1. **Confirm slug `<proposed>`?** — _How to pick:_ Yes if it captures the task in 2-4 hyphenated words. No if it collides with an existing slug or is too generic. Default: derived from prompt nouns; date-prefix appended if collision.
