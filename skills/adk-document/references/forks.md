# adk-document — fork IDs

| fork_id | options | recommendation |
|---|---|---|
| `tone` | engineer / pm / exec / mixed | matches `--audience`; default engineer |
| `audience` | engineer / pm / exec / mixed | engineer |
| `template` | per-type catalog | per-type default |
| `length-target` | one-pager / 2-page / full / brief | per-type default |
| `citation-density` | every-claim / non-obvious-only | non-obvious-only for runbooks; every-claim for RCA / ADR |
