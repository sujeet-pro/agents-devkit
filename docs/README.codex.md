# AKIT For Codex

AKIT uses Codex native skill discovery.

## Quick Install

Tell Codex:

```text
Fetch and follow instructions from https://raw.githubusercontent.com/sujeet-pro/claude-devkit/refs/heads/main/.codex/INSTALL.md
```

That flow clones the repo to `~/.akit` and exposes `skills/` at `~/.agents/skills/akit`.
