---
title: 'settings.json'
description: 'Plugin-level Claude defaults for the adk plugin.'
artifact_kind: config
---

# settings.json

Plugin-level Claude Code defaults applied automatically when the `adk` plugin is loaded.

## Current contents

```json
{
  "subagentStatusLine": "[adk] {agent} - {model}"
}
```

## Source

`settings.json` at repo root.
